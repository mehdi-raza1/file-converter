version: '3.8'

services:
  file-converter:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_ENABLE_CORS=false
    restart: unless-stopped